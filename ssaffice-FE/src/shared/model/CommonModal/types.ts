export type ModalName = 'EmailValidFalse' | 'LoginFail'

export type ModalElement = {
  modal: JSX.Element
  width: string
  height: string
  hasShadow?: boolean
  hasCloseButton?: boolean
}
